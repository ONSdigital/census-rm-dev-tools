# Unaddressed QID Batch Generator

A python script to tell RM to generate unaddressed QID/UAC pairs from a CSV config file.

## Dependencies

Install dependencies with 
```bash
pipenv install --dev
```
